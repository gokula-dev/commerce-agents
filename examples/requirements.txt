# Backend deps for the Logitech demo (examples/logitech), Vercel service root = examples/.
# Local packages are editable, relative to this file; pip resolves their own pinned
# transitive deps (anthropic, pydantic, fastapi, uvicorn, python-dotenv, etc.) itself.
# httpx is only for poc/logitech_ingest/typesense_export.py, run by the Vercel build step
# (see pyproject.toml [tool.vercel.scripts]) — not a runtime dependency of the app itself.
# merchant-agent/core is required even though this backend is shopping-only: demo_common's
# __init__.py unconditionally imports both roles' merchant.py at package load time.
-e ../commerce-common[examples]
-e ../shopping-agent/core
-e ../shopping-agent/runtime-messages-api
-e ../merchant-agent/core
-e ../merchant-agent/runtime-messages-api
httpx>=0.28
